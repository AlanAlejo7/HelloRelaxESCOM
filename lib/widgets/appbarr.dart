import 'package:flutter/material.dart';

void main() => runApp(const Barr());

class Barr extends StatelessWidget {
  const Barr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
